# Local Aspire Dashboard API key for the full local stack.
# Copy this file to .env and set a strong random value.
# The real .env is gitignored and must never be committed.
#
# Generate a strong key, for example:
#   openssl rand -base64 32
ASPIRE_DASHBOARD_API_KEY=replace-with-a-strong-random-key
